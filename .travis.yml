language: ruby
cache: bundler
rvm:
  - 2.6 # testing ruby26

before_install:
  - gem install bundler
  - bundle install

gemfiles:
  - Gemfile

branches:
  only:
    - master

services:
  - elasticsearch
  - redis-server

before_script:
  - cp config/secrets.yml.dist config/secrets.yml
  - sleep 10
  
